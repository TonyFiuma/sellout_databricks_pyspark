# Synthetic demo input

The real parsing/import notebooks are intentionally shipped without production extracts.
To demonstrate the portfolio Gold layer independently, run `00_create_synthetic_harmonized.py`.
It creates a small synthetic `silver.harmonized` table, including two submissions for the same
company/month so the latest-submission Gold rule can be observed.
